FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /web

COPY ./web ./web
RUN pip install -r ./web/requirements.txt

COPY ./ml ./ml
RUN pip install -r ./ml/requirements.txt

CMD uvicorn web.main:web \
    --host 0.0.0.0 \
    --port 8000 \
    --reload
